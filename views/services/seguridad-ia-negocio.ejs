<!DOCTYPE html>
<html lang="es">
<head>
  <%- include('../partials/head') %>
</head>
<body>
  <%- include('../partials/gtm-body') %>
  <%- include('../partials/header') %>

  <section class="service-hero service-hero-ai service-hero-photo">
    <img class="service-hero-bg" src="<%= modo.imagen %>" alt="" loading="eager">
    <div class="service-hero-overlay"></div>
    <div class="container service-hero-inner">
      <div class="service-hero-icon"><svg viewBox="0 0 24 24"><%- icons[modo.icono] %></svg></div>
      <div>
        <nav class="breadcrumb" aria-label="Ruta de navegación">
          <a href="/servicios">Servicios</a> ›
          <a href="/servicios/bloque/seguridad-ia-naves-fincas">Naves y Fincas</a>
        </nav>
        <span class="eyebrow">Seguridad IA · Naves y Fincas — <%= modo.subtitulo %></span>
        <% if (modo.titular) { %><p class="hero-titular"><%= modo.titular %></p><% } %>
        <h1><%= modo.nombre %></h1>
        <p><%= modo.resumen %></p>
        <a href="https://wa.me/34<%= empresa.whatsapp %>?text=Hola%2C%20quiero%20informaci%C3%B3n%20sobre%20Seguridad%20IA%20para%20mi%20nave%20o%20finca" target="_blank" rel="noopener" class="btn btn-whatsapp cta-block">
          Pedir presupuesto gratuito
        </a>
      </div>
    </div>
  </section>

  <section class="section">
    <div class="container">
      <h2 class="sub-heading">Ideal para</h2>
      <ul class="check-list check-list-lg">
        <% modo.idealPara.forEach(function(item){ %>
          <li><%= item %></li>
        <% }) %>
      </ul>

      <div class="ia-modo-info-box">
        <p><strong>Requiere el Mini-PC IA Central</strong></p>
        <p>Seguridad IA para Naves y Fincas es un servicio independiente de la Plataforma IA Predictiva residencial, pero reutiliza el mismo Mini-PC IA Central (desde 590 €, obligatorio) y el mismo motor Python de detección — no necesitas comprar un equipo aparte. Los precios de abajo son el coste de las cámaras, la configuración del modelo de detección y la puesta en marcha, a la escala de una nave, almacén o finca.</p>
      </div>

      <% if (modo.video) { %>
        <div class="modo-video-wrap">
          <video class="modo-video" controls preload="none" poster="<%= modo.imagen %>">
            <source src="<%= modo.video %>" type="video/mp4">
          </video>
        </div>
      <% } %>

      <% modo.ejemplos.forEach(function(ej){ %>
        <div class="ia-example-block">
          <h2 class="sub-heading">Ejemplo — <%= ej.titulo %></h2>
          <p class="example-client"><%= ej.subtitulo %></p>

          <% if (ej.imagen && ej.imagenAntes) { %>
            <div class="pack-photo-pair">
              <div class="pack-photo-wrap">
                <span class="photo-label">Antes</span>
                <img src="<%= ej.imagenAntes %>" alt="Antes — <%= ej.titulo %>" loading="lazy">
              </div>
              <div class="pack-photo-wrap">
                <span class="photo-label photo-label-after">Después</span>
                <img src="<%= ej.imagen %>" alt="Después — <%= ej.titulo %>" loading="lazy">
              </div>
            </div>
          <% } %>

          <div class="options-stack options-stack-3">
            <% ej.opciones.forEach(function(op){ %>
              <%- include('../partials/option-card', { op: op, fmt: fmt }) %>
            <% }) %>
          </div>
        </div>
      <% }) %>

      <% if (modo.nota) { %><p class="example-note"><%= modo.nota %></p><% } %>

      <div class="two-col two-col-top">
        <div>
          <h2 class="sub-heading">Extras habituales</h2>
          <ul class="plus-list">
            <% modo.extras.forEach(function(item){ %>
              <li><%= item %></li>
            <% }) %>
          </ul>
        </div>
        <div>
          <h2 class="sub-heading">Clientes que contrataron este servicio también instalaron</h2>
          <ul class="dot-list">
            <% modo.tambienInstalaron.forEach(function(item){ %>
              <li><%= item %></li>
            <% }) %>
          </ul>
        </div>
      </div>

      <p class="orientative-note">Todos los importes son orientativos. El precio definitivo se confirma tras la visita técnica gratuita.</p>
    </div>
  </section>

  <section class="section final-cta">
    <div class="container final-cta-inner">
      <h2>¿Hablamos de la vigilancia de tu nave o finca?</h2>
      <p>Primera visita gratuita. Presupuesto sin compromiso.</p>
      <a href="https://wa.me/34<%= empresa.whatsapp %>?text=Hola%2C%20quiero%20informaci%C3%B3n%20sobre%20Seguridad%20IA%20para%20mi%20nave%20o%20finca" target="_blank" rel="noopener" class="btn btn-whatsapp btn-lg">
        <%= empresa.whatsappDisplay %>
      </a>
    </div>
  </section>

  <%- include('../partials/footer') %>
</body>
</html>
